# Velxor — devC 트리 STATUS Snapshot

> **작성일**: 2026-05-22 (Asia/Seoul) · **작성자**: Worker C 컨텍스트
> **트리**: `/home/lsy/src/Velxor` (현재 HEAD branch: `main`, 8f2ed61)
> **범위**: 본 문서는 devC working tree(현재 main 체크아웃 상태)에 존재하는 파일 전수 검토 결과의 단일-스냅샷이다. plan/Velxor embedded repo 와 다를 수 있다.
> **관련**: [`contracts/interface-schema.md`](./contracts/interface-schema.md) · [`role-assignment.md`](./role-assignment.md) · [`velxor-consensus-plan.md`](./velxor-consensus-plan.md)

---

## 0. Executive Summary

Walking Skeleton AC1 의 4-레이어가 **부분 통과** 상태. `run-all.sh` 패치 후 engine + rust-service + UI 3개 컴포넌트가 모두 부팅 가능함은 확인됐으나, **3개 결정적 결함**으로 인해 AC1 (`node_add{event_type:"FileWrite"}` ws-record 캡처) 종단 검증은 아직 통과하지 않음:

1. **rust-service가 root 없이 실행되면 fanotify EPERM 으로 즉시 종료** (logs/trace.json line 3 증거). `VELXOR_STUB` 미지정 시 stub 분기를 안 타고 real fanotify 분기로 빠짐 → 일반 사용자 실행 시 데이터 흐름 단절.
2. **UI(App.tsx)가 Vite 기본 boilerplate 그대로** — ProcessTree·DetailPanel·WS client 미구현. B의 본격 작업이 아직 시작되지 않은 상태.
3. **`aggregator.rs` 가 빈 placeholder** (`pub fn placeholder() {}`) — collector_source → classifier_client → WS broadcast 의 중간 brain 이 비어 있어 `/classify` 호출이 한 번도 발생하지 않음.

또한 schema-vs-code drift 4건 발견 (§5.3 표). 그러나 `run-all.sh` self-contained 가드 5종 패치로 fresh-clone 재현성은 한 단계 상승. `c-review-notes.md` 작성 + `aggregator` 본구현이 Week 1 종료 전 critical path.

**한 줄 평**: 인프라 7할, 컨트랙트 8할, 본 기능 3할. AC1까지 남은 가장 짧은 경로는 **(a) collector_source stub 분기에 VELXOR_STUB=collector 강제 실행 + (b) aggregator → classifier_client 한 줄 wire-up**.

---

## 1. Git 상태

### 1.1 현재 HEAD

```
branch       : main (origin/main 동기)
HEAD commit  : 8f2ed61  C: week0 venv + flask /health + waitress threads=4
parent merge : 5d13fcd  Merge branch 'main' of github.com:fireapple1/Velxor
```

### 1.2 최근 10개 커밋 (시간 역순)

| Hash | 작성자 의도 | 의미 |
|---|---|---|
| 8f2ed61 | `C: week0 venv + flask /health + waitress threads=4` | C의 engine stub (현 HEAD) |
| 5d13fcd | `Merge branch 'main' of github.com:fireapple1/Velxor` | 동기화 merge |
| 0a14177 | `.` | C의 작업 푸시 |
| 541c16c | `.` | (가벼운 푸시) |
| 084bda4 | `A: week1 4 rust stubs` | rust-service 4 모듈 + Cargo.toml |
| 873658f | `interface-schema v1-draft (Rust DRI sections)` | **v1-draft 발행** (review trigger 기준점) |
| 2e18e95 | `.` | (가벼운 푸시) |
| 2fd3cce | `edit role` | role-assignment.md 수정 |
| 10539d3 | `edit role` | (계속) |
| bc2ace9 | `Merge pull request #8 from fireapple1/fuckdevC` | (의도 불명, 브랜치명 비표준) |

### 1.3 브랜치 토폴로지

```
local branches: devC, main(*)
remote branches: origin/main, origin/devA, origin/devB, origin/devC, origin/fuckdevC
```

⚠️ `origin/fuckdevC` 가 정리되지 않은 채 남아 있음. devC 와 별개의 정리 안 된 브랜치. 다음 cleanup 시 삭제 후보.

### 1.4 Uncommitted 변경 (working tree)

```
M  scripts/run-all.sh             (staged + further unstaged — 패치 본체)
 M ui/package-lock.json           (npm install 부산물, libc 필드 strip)
?? rust-service/logs/             (cargo run 시 trace.json 출력 디렉토리)
```

#### scripts/run-all.sh diff stat
```
1 file changed, 40 insertions(+), 3 deletions(-)
```
**5종 self-contained 가드 추가** (cd to repo root / mkdir logs / port pre-flight / npm install fallback / 2-step SIGTERM→SIGKILL trap). 자세히는 §11.1.

#### ui/package-lock.json diff
의도하지 않은 30 라인 삭제 (npm 의 `libc:["glibc"|"musl"]` optional 필드 strip). 현재 Node 20.20.2 가 본 lockfile 이 만들어진 환경(아마 더 신상 Node)과 다른 npm 버전이라 발생한 incidental. 기능 영향 없음. **커밋 여부 결정 필요** — 보통 `git checkout -- ui/package-lock.json` 으로 원복 권장.

### 1.5 Stage 정리 필요

`scripts/run-all.sh` 가 staged + unstaged 혼합 상태. `git restore --staged scripts/run-all.sh && git add scripts/run-all.sh` 로 한 번에 stage 후 커밋하는 게 깔끔.

---

## 2. 빌드 / 런타임 상태

### 2.1 Python engine (C 소유)

| 항목 | 상태 | 비고 |
|---|---|---|
| `python-engine/.venv/` | ✅ 존재 (Python 3.12.3) | `/usr/bin/python3 -m venv` 로 생성 |
| `requirements.txt` 일치 | ✅ 18 패키지 모두 lock | `pip list ≡ requirements.txt` |
| Flask 3.0.3, Waitress 3.0.2, scikit-learn 1.4.2, numpy 1.26.4 | ✅ | ENVIRONMENT.md §3 핀과 동일 |
| `requirements.lock` 발행 | ❌ | Week 0 종료 전 만들어두면 AC 재현성 ↑ |
| `app.py` `/health` + `/classify` | ✅ stub 응답 정상 | 단 `model_version` schema drift (§5.3) |
| 단독 smoke test | ✅ `curl /classify` → 200 OK 4-key | 좀비 정리 후 |

### 2.2 Rust service (A 소유)

| 항목 | 상태 | 비고 |
|---|---|---|
| `Cargo.toml` 의존성 | ✅ tokio 1.52, tokio-tungstenite 0.29, reqwest 0.13, nix 0.31 +fanotify | features=full |
| `target/release/rust-service` | ✅ 3.3 MB 바이너리 빌드됨 | `cargo build --release` 35.13s |
| `cargo check` warnings | (미확인) | next: `cargo clippy` 한 번 권장 |
| 단독 실행 (root 아님) | ❌ **EPERM** | `Error: EPERM: Operation not permitted` (logs/trace.json:3) — fanotify_init 실패 |
| `VELXOR_STUB=collector` 강제 시 | ✅ (예상) | `collector_source.rs:8-10` 의 stub 분기가 fanotify 우회 |
| `ws_broadcaster` 단독 보내기 | ✅ ws server listening port 7000 (트레이스 확인) | `main.rs:16` |

### 2.3 UI (B 소유)

| 항목 | 상태 | 비고 |
|---|---|---|
| `package.json` 의존성 명세 | ✅ React 19.2 + Vite 8 + Electron 42 + @xyflow/react 12 | xyflow는 ProcessTree 용 |
| `node_modules` | ✅ 191 packages 설치됨 | 5월 22일 npm install |
| Node 버전 충돌 | ⚠️ **`EBADENGINE`** | 현재 Node 20.20.2 / electron 42 는 ≥22.12 요구 |
| `vite` dev 기동 | ✅ ready in 162 ms on :5173 | `npm run dev` |
| `App.tsx` 본 구현 | ❌ **Vite 기본 boilerplate** | `<button>Count is {count}</button>` — B 미시작 |
| `main.tsx` | ✅ React 19 createRoot StrictMode | 표준 |
| WS client / dedupe / gap 처리 | ❌ 미구현 | 본 작업 0% |
| `tsconfig.json` | ✅ | 변경 없음 (Vite 기본) |

### 2.4 Scripts (C + A 공동)

| 파일 | 소유 | 상태 |
|---|---|---|
| `scripts/run-all.sh` | C | ✅ 패치됨 (5종 가드, §11.1) — 단 staged/unstaged 혼합 |
| `scripts/ws-record.sh` | A | ✅ 작성됨 — 5초 WS 캡처 + grep `node_add` + `FileWrite` → exit 0/1 |
| `scripts/ac6-verify-block.sh` | A (예정) | ❌ 미작성 (Week 8-9 책임) |
| `scripts/poc-bench.sh` | C (예정) | ❌ 미작성 (Week 6-7 책임) |

### 2.5 포트 점유 / 좀비

직전 smoke test 후 잔류 가능 (vite 손자 프로세스 trap 누락 issue 미해결):

```
:5173  node vite           ← UI 잔류 (npm→sh→node 체인의 손자)
:8765  waitress             ← engine 잔류 가능
:7000  (rust-service 죽음)  ← EPERM으로 즉시 종료, 잔류 X
```

**상시 cleanup**: `pkill -u $USER -f 'waitress_conf|vite' 2>/dev/null`

---

## 3. 파일 인벤토리 (devC 트리, 5399 LoC)

### 3.1 코드 산출물 (실제 구현)

| 파일 | LoC | 소유 | 상태 | 비고 |
|---|---|---|---|---|
| `rust-service/src/main.rs` | 24 | A | 🟢 기본 골격 | tokio main + broadcast::channel(1024) + spawn ws + collector_source. **classifier_client / aggregator 미연결.** |
| `rust-service/src/ws_broadcaster.rs` | 105 | A | 🟢 본 구현 | ReplayBuffer (5s sliding) + last_seq=N handshake + replay backlog + Lagged 처리. TODO 2건 (§4.4 subscribe 순서 race, gap emit). |
| `rust-service/src/collector_source.rs` | 63 | A | 🟢 stub 분기 + 진입점 | VELXOR_STUB 분기 + `events.jsonl` poll(200ms) + partial-line guard + dropped counter. real fanotify 분기는 §4.2 호출만. |
| `rust-service/src/fanotify_adapter.rs` | 177 | A | 🟢 **Week 4-5 분량 본 구현** | nix::sys::fanotify Init+Mark+ReadEvents + build_payload + /proc readlink + ts_unix_ms + saturating dropped. A가 일정보다 앞서감. EPERM 실패는 root 권한 문제. |
| `rust-service/src/classifier_client.rs` | 14 | A | 🟡 한 함수만 | `classify(events, window_ms) → /classify` POST. timeout 200ms. **#[allow(dead_code)] — 아직 호출 안 됨.** |
| `rust-service/src/aggregator.rs` | 3 | A | 🔴 **빈 placeholder** | `pub fn placeholder() {}` — 본 구현 0%. sliding window + burst 감지 + classify 호출 로직 부재. **AC1 → AC4 의 중간 brain.** |
| `python-engine/app.py` | 22 | C | 🟢 stub 응답 | Flask `/health` + `/classify` 하드코드 4-key. `MODEL_VERSION = "stub-v1" if VELXOR_STUB=="engine" else "rule-based-v1"`. |
| `python-engine/fallback_rules.py` | 24 | C | 🟢 rule skeleton | `write_rate ≥ 50/s → ransomware 0.9` else `benign 0.6`. **app.py에서 아직 호출 안 됨** (Week 6+ 통합 예정). |
| `python-engine/waitress_conf.py` | 5 | C | 🟢 | `serve(app, '127.0.0.1', 8765, threads=4)` |
| `ui/src/App.tsx` | 123 | B | 🔴 **Vite 기본 boilerplate** | React 로고 + 카운터 버튼 + 데모 링크. **B 미시작.** |
| `ui/src/main.tsx` | 11 | B | 🟢 standard | `createRoot + StrictMode`. |
| `ui/vite.config.ts` | 8 | B | 🟢 standard | `plugins: [react()]`. |
| `scripts/run-all.sh` | 85 (post-patch) | C | 🟢 self-contained 가드 5종 | (§11.1 참조) |
| `scripts/ws-record.sh` | 25 | A | 🟢 본 구현 | websocat 5초 + grep PASS/FAIL. AC1 자동 판정. |

### 3.2 Spec / 계획 문서 (devC 트리에 동기된 사본)

| 파일 | LoC | 소유 | devC 상태 | 비고 |
|---|---|---|---|---|
| `README.md` | 53 | A | 🟢 4계층 ubuntu 마이그레이션 반영 | Status 라인: "🟡 계획 단계" — 코드 시작됐으므로 갱신 후보 |
| `ENVIRONMENT.md` | 119 | A | 🟢 24.04 + Python 3.12 + Rust 1.78 + Node 20 + bash 5.2 | Node 20 vs Electron 42 (≥22 요구) 불일치 — 추후 조정 |
| `role-assignment.md` | 387 | C 주관 | 🟢 2026-05-22 재분배 반영 (A=62h / B=32h / C=45h) | plan/Velxor 사본과 동기 추정 |
| `velxor-consensus-plan.md` | 405 | 합의 | 🟢 Iteration 5 ADR 포함 | plan/Velxor 사본과 동기 추정 |
| `worker-A-timeline.md` | 905 | A | 🟢 Critical path 단일점 + ws-record.sh + ac6-verify-block.sh 사양 | plan/Velxor 사본 |
| `worker-A-todo.md` | (미확인) | A | (미확인) | A의 일일 todo. devC 트리에 존재. |
| `worker-B-timeline.md` | 653 | B | 🟢 UI 풀스택 + 발표 퀄리티 흡수 | 이행 시작 안 됨 (App.tsx boilerplate) |
| `worker-C-timeline.md` | 968 + 패치 노트 추가 | C | 🟢 run-all.sh + Python DRI + ARCHITECTURE.md + 2026-05-22 patch note | §2.5 doc sync 완료 (plan/Velxor 측) |
| `worker-C-tasks.md` | (미확인) | C | (미확인) | C의 일일 task. devC 트리에 존재. |
| `deep-interview-spec.md` | (미확인) | 합의 | 🟢 historical | 6-round Socratic Q&A 산출물, final ambiguity 17.8% |

### 3.3 Contracts

| 파일 | LoC | 소유 | 상태 |
|---|---|---|---|
| `contracts/interface-schema.md` | 221 | A 주관 | 🟢 v1-draft @ 873658f. 7개 섹션 + 책임 매핑. |
| `contracts/v1.1-review-trigger.md` | 68 | A | 🟢 발행됨 (데드라인 2026-05-24). A 자체 노트 4건 포함. |
| `contracts/c-review-notes.md` | — | C | ❌ **미작성** — Week 1 마감 전 책임 |

### 3.4 트리에는 없으나 로드맵상 신규

| 예정 산출물 | 소유 | due | 비고 |
|---|---|---|---|
| `docs/ARCHITECTURE.md` | C | Week 7 | 4-layer diagram + VELXOR_STUB sweep + Walking Skeleton 의의 |
| `docs/electron-ws-footgun.md` | B | Week 2-3 | Electron dev hot-reload + WS double-mount issue |
| `docs/DEMO-SCRIPT.md` | B | Week 8-9 | 60초 narration 시나리오 |
| `evidence/AC3/frame-1000ms.png` | B | Week 8-9 | OBS 1920x1080 60fps → ffmpeg frame extract |
| `evidence/REHEARSAL-LOG.md` | B 주관 | Week 8-9 | 3회 리허설, 본인 + 팀 응답시간 기록 |

---

## 4. 컴포넌트별 진행률 (per-worker)

### 4.1 Rust service (A) — 62h 중 ~12h 추정 소비

```
계획:  Week 0-1 = 25h (smoke + scaffold + 4 stub + interface-schema + ws-record)
실제:  fanotify_adapter.rs 본 구현 + ws_broadcaster 본 구현 + collector_source 본 구현 + ws-record.sh
       (Week 4-5 분량 일부 선행 — A 가 schedule 앞당김)
```

| 모듈 | LoC | 진행률 | 잔여 |
|---|---|---|---|
| `main.rs` (런타임 부트) | 24 | 60% | classifier_client / aggregator wire-up |
| `ws_broadcaster.rs` | 105 | 90% | §4.4 race / gap emit TODO 2건 |
| `collector_source.rs` | 63 | 75% | stub branch OK, real branch는 fanotify EPERM 의존 |
| `fanotify_adapter.rs` | 177 | 85% | Week 4-5 분량 선행, root 권한 + capability 처리 필요 |
| `classifier_client.rs` | 14 | 50% | 함수만 있음 — aggregator에서 호출 부재 |
| `aggregator.rs` | 3 | **5%** | 🔴 **Critical path 단일점 — 빈 placeholder.** |
| `interface-schema.md` | 221 | 90% | v1-draft 발행됨, v1.1 collation 대기 |
| `ws-record.sh` | 25 | 100% | ✅ |
| `ac6-verify-block.sh` | — | 0% | Week 8-9 |

**A의 단일 점 risk 가시화**: `aggregator` 가 빈 채면 AC1 두 번째 게이트 ("ws-record 캡처에 `node_add{event_type:"FileWrite"}`")는 패스 가능 (collector_source가 직접 broadcast 송신하므로), 그러나 AC4 (p99<100ms) 측정의 입력 분포·classify 호출 빈도 미정 → C의 측정 환경 불안정.

### 4.2 Python engine (C) — 45h 중 ~6h 추정 소비

```
계획:  Week 0-1 = 7h (venv + flask /health + waitress threads=4 + classify stub + fallback_rules skeleton + run-all.sh + Python DRI 검토)
실제:  app.py(/health+/classify stub) + fallback_rules(rule skeleton) + waitress_conf + run-all.sh(5종 가드 패치)
잔여:  c-review-notes.md 작성 + commit 정리
```

| 산출물 | 진행률 | 잔여 |
|---|---|---|
| `app.py` `/health` | 100% |  |
| `app.py` `/classify` stub | 100% | (model_version drift 해소 필요, §5.3) |
| `fallback_rules.py` | 100% | (Week 6+ app.py 통합 시 wire-up) |
| `waitress_conf.py` | 100% |  |
| `scripts/run-all.sh` | 95% | commit 정리만 남음 |
| Python DRI Week 1 검토 | 80% | `c-review-notes.md` 작성 후 PR comment 제출 |
| `docs/ARCHITECTURE.md` | 0% | Week 7 |
| `poc-bench.sh` / `eval-*.sh` | 0% | Week 6-7 |
| p99 측정 (AC4) | 0% | Week 6.7 |
| held-out v3 평가 (AC5) | 0% | Week 8-9 |

### 4.3 UI (B) — 32h 중 ~2h 추정 소비

```
계획:  Week 0-1 = 6h (vite + react + electron + @xyflow/react scaffold + UI stub + WS client)
실제:  npm install + vite dev 기동 확인. App.tsx 본 구현 0%.
잔여:  ProcessTree (xyflow) + DetailPanel + BlockButton + WS client (?last_seq=N + dedupe + gap full refresh)
```

| 산출물 | 진행률 | 잔여 |
|---|---|---|
| Vite scaffold + npm deps | 100% |  |
| `App.tsx` 본 구현 | **0%** | 🔴 Vite 기본 boilerplate 상태 |
| `components/ProcessTree.tsx` (@xyflow/react + dagre) | 0% | Week 1-3 |
| `components/DetailPanel.tsx` | 0% | Week 1-3 |
| `components/BlockButton.tsx` | 0% | Week 8-9 (AC6 의존) |
| `components/Timeline.tsx` | 0% | **deferral order #2** — 잔여 시간 부족 시 제외 |
| `ws/client.ts` (?last_seq=N + dedupe + gap) | 0% | Week 1-3 |
| `api/block.ts` (POST /block/:pid) | 0% | Week 8-9 |
| `electron/main.ts` | 0% | Week 4-7 |
| `electron-ws-footgun.md` | 0% | Week 2-3 |
| `DEMO-SCRIPT.md` (60s narration) | 0% | Week 8-9 |
| `evidence/AC3/frame-1000ms.png` | 0% | Week 8-9 |
| `REHEARSAL-LOG.md` | 0% | Week 8-9 |

⚠️ **B의 underload risk 재발 신호**: Week 1 마감 시점에 App.tsx 본 구현이 비어 있다는 건 재분배(2026-05-22)의 의도(B가 UI 풀스택을 깊이 가져가게)와 충돌. 자체 캐치업 또는 timeline 재조정 필요.

---

## 5. Interface Contract 상태

### 5.1 v1-draft @ 873658f — 발행 완료

`contracts/interface-schema.md` 8개 섹션:

1. Collector → Service: `BehaviorEventV1` (§1, schema_version/seq/dropped_since_last/pid/parent_pid/image_path/event_type/file_path/volume_id?/op_detail?/ts_unix_ms)
2. Service → Engine: `POST /classify` (§2, request{events,window_ms} / response 4-key)
3. Service → UI: `WebSocket WsMessage` (§3, schema_version/seq/type/payload)
4. WS Reconnect 프로토콜 (§4, ?last_seq=N + replay → live + gap 처리)
5. Evolution Policy (§5, v1.x additive only)
6. `VELXOR_STUB` Semantics (§6, unset/collector/engine/both 4모드)
7. 결정적 책임 매핑 (§7)
8. 변경 이력 (§8)

### 5.2 v1.1 review trigger (2026-05-22 발행, 데드라인 2026-05-24)

A가 `contracts/v1.1-review-trigger.md` 로 B/C 에 review 요청.

**A 자체 노트 4건** (이미 반영 후보):
1. `op_detail` 변종별 모양 추가 (FileRename `{src_path, dst_path}`, ProcessCreate `{argv, cwd}`)
2. `dropped_since_last` u32 포화 정책 (`dropped_saturated: bool` flag)
3. `/classify` 에러 응답 shape (`{error: string, retry_after_ms?: u32}`)
4. `image_path` readlink fallback (`<unknown:pid=N>` sentinel) — **이미 `fanotify_adapter.rs:160-164` 에 구현됨**

### 5.3 Schema vs 실제 코드 drift (현 시점)

| 항목 | schema 명세 | 실제 구현 | 평가 |
|---|---|---|---|
| `/health` endpoint | **schema 없음** | `app.py:9-11` 에 구현 | ❌ schema 누락 — C가 v1.1 추가 요청 |
| `model_version` 컨벤션 | `lr-<ver>` / `rules-fallback-<ver>` | `stub-v1` / `rule-based-v1` | ❌ prefix drift — C의 v1.1 응답에 포함 |
| `events[]` 길이 / body 상한 | **명세 없음** | 무제한 | ❌ p99 측정 변수 — C가 v1.1 추가 요청 |
| 빈 events 처리 | **명세 없음** | stub: 항상 ransomware / rule: benign | ❌ 모드별 분기 — C가 v1.1 추가 요청 |
| `BehaviorEventV1.payload.seq` | inner seq (§1.1) | `fanotify_adapter.rs:132` 에서 `null` (outer WsMessage 가 canonical) | ⚠️ 중첩 분리 정책 — 의도된 것이라 표기만 명확화 |
| `event_type` enum 3종 | `FileWrite \| FileRename \| ProcessCreate` | `fanotify_adapter.rs:143-155` 모두 cover | ✅ 일치 |
| WS dedupe + gap full refresh | B 책임 | UI 미구현 | 🟡 B 작업 대기 |

### 5.4 C가 Week 1 마감 전 제출할 것

`contracts/c-review-notes.md` (양식: A의 v1.1-review-trigger.md 와 동일):

```
필드명 / 이유 / additive 제안   ×  4건
1. GET /health endpoint 누락
2. POST /classify request body 상한 미정
3. empty events 처리 정책
4. model_version stub/rules prefix 컨벤션
```

A의 자체 노트와 중복되는 `/classify` 에러 응답 shape 은 별도 제출 X (A 가 이미 알고 있음).

---

## 6. AC1 Walking Skeleton 진행률

consensus-plan §AC 정의:
> AC1 — `scripts/run-all.sh` exit 0 + `scripts/ws-record.sh` 캡처에 `node_add { event_type: "FileWrite" }` 메시지 + 3인 재현 + `git tag walking-skeleton-v1`

### 6.1 4-레이어 게이트

| Layer | 검증 | 현 상태 |
|---|---|---|
| L1: `run-all.sh` exit 0 | `set -euo pipefail` 무결성 | ✅ 패치 후 통과 (smoke 2026-05-22 19:38) |
| L2: 3개 컴포넌트 health | engine /health 200 + ws :7000 listen + vite :5173 응답 | 🟡 engine + vite ✅, ws **EPERM**으로 죽음 (stub 모드 강제 안 됨) |
| L3: End-to-end 데이터 흐름 | events.jsonl smoke → collector_source → broadcast → WS | 🟡 stub 분기는 가능, real 분기는 EPERM 차단 |
| L4: `ws-record.sh` PASS | grep `"node_add"` AND grep `"FileWrite"` on capture | ❌ 아직 시도 안 함 (실제 데이터 흐른 적 없음) |

### 6.2 AC1 통과까지의 최단 경로

```
(a) VELXOR_STUB=collector  ./scripts/run-all.sh       # fanotify EPERM 우회
(b) ./scripts/ws-record.sh > /tmp/ws-capture.jsonl    # 5초 캡처 + 자동 판정
```

이 두 명령으로 L1-L4 일괄 통과 가능. **단**, `aggregator.rs` 가 비어 있어 `node_add` 가 *type* 으로만 흐르고 `/classify` 호출은 발생하지 않음 → AC4 (p99<100ms) 측정의 입력은 별도 마련 필요. AC1 기준 자체로는 통과 가능.

### 6.3 3인 재현성 (AC1 조건 c)

| 조건 | 상태 |
|---|---|
| A 머신에서 `run-all.sh` 동일 결과 | (확인 필요) — `htA/Velxor` 트리에서 동일 패치 fetch 후 검증 |
| B 머신에서 `run-all.sh` 동일 결과 | (확인 필요) — UI 부팅까지만 보면 됨 |
| C 머신에서 `run-all.sh` 동일 결과 | ✅ 본 트리에서 검증됨 |

push 후 A·B 가 pull → 자기 환경에서 한 번씩 돌려야 AC1 c 조건 충족. **현재 patch가 commit/push 안 됨** → 3인 재현 시작점 부재.

### 6.4 tag push 권한

재분배 후 `walking-skeleton-v1` tag owner = **C**. A·B 는 fetch 만. 따라서 위 6.2 → 6.3 검증 후 C 가:

```bash
git tag walking-skeleton-v1 -m "AC1: run-all.sh + ws-record.sh PASS, 3인 재현"
git push --tags
```

---

## 7. AC2-AC8 진척 매트릭스

velxor-consensus-plan.md 의 AC 8종 ↔ 현 코드 매핑.

| AC | 정의 (요약) | 책임 | 진행률 | 핵심 산출물 / 의존 |
|---|---|---|---|---|
| AC1 | Walking Skeleton (run-all + ws-record + 3인 재현 + tag) | C(주)/A | 65% | §6 참조 |
| AC2 | Stub Retention Gate (VELXOR_STUB unset/collector/engine/both 4 모드 영속) | 전원 | 50% | run-all.sh sweep 자동화 — C |
| AC3 | UI 첫 frame ≤ 1000 ms 시연 (1920x1080 60fps + ffmpeg evidence) | B | 0% | OBS + DEMO-SCRIPT — B Week 8-9 |
| AC4 | `/classify` p99 < 100 ms (event → ws 2 timestamp tracing 간소화) | C(측정) / A(instrumentation) | 5% | tracing JSON line 발견됨, p99 측정 인프라 부재 |
| AC5 | held-out v3 (.pdf → .locked) ≥ 9/10 + 임계값 완화 X (honest reporting) | C | 0% | 학습 모델 + held-out 데이터 Week 6-7 |
| AC5c | "synthetic PoC, not real-world malware" disclaimer 슬라이드 의무 | B | 0% | 슬라이드 |
| AC6 | 자동 차단: kill(SIGTERM) → 200ms → SIGKILL + `kill -0 $PID` 검증 | A | 0% | block_handler 미구현, `ac6-verify-block.sh` 미작성 |
| AC7 | 1시간 sustained run, 메모리 leak 부재 | A | 0% | Week 8-9 |
| AC8 | 3-mode sweep CI (collector / engine / both 영속) | A(collector) / C(engine) / B(UI ack) | 30% | run-all.sh 가드 ✅, sweep 자동화 미작성 |

---

## 8. 미해결 결정 / Blocker

### 8.1 Hard blocker (Week 1 마감 직접 영향)

1. **`rust-service` root 권한 처리 정책**
   - 옵션 A: `sudo setcap cap_sys_admin+ep target/release/rust-service` (1회만, 깔끔)
   - 옵션 B: `sudo ./target/release/rust-service` (수동, 매번)
   - 옵션 C: `VELXOR_STUB=collector` 영속, real 분기는 Week 4+ 까지 비활성
   - **권장**: AC1 통과 위해 옵션 C로 walking-skeleton-v1 태그 발행, real fanotify 는 Week 4 시작 시 옵션 A로 전환
   - **owner**: A 결정, C/B 영향 받음 → consensus-plan ADR 후보

2. **`aggregator.rs` 빈 placeholder**
   - Week 1 마감 전 최소 sliding window + classifier_client 호출 wire-up 필요
   - 그렇지 않으면 AC4 측정 입력 부재
   - **owner**: A. Week 1 내 ~4h 추가 흡수 또는 Week 4-5 로 이월

3. **B App.tsx 본 구현 시작 시점**
   - Week 1 마감 = 2026-W1 종료, 즉 며칠 내. boilerplate 상태에서 ProcessTree+DetailPanel 0% → critical
   - **owner**: B. Week 1 내 최소 stub UI + WS client 1차 draft 필요

### 8.2 Soft blocker (Week 2-3 영향)

4. **`ui/package-lock.json` 의도하지 않은 변경**
   - `libc:["glibc"|"musl"]` optional 필드 30 라인 strip — Node 20.20.2 vs lockfile 작성 시점 환경 차이
   - 권장: `git checkout -- ui/package-lock.json` 으로 원복, 진짜 dep 변경 시에만 lockfile 커밋

5. **Node 버전 vs Electron 42 호환성**
   - ENVIRONMENT.md 핀: Node 20 LTS / 실제: 20.20.2 / Electron 42 요구: ≥22.12
   - 영향: walking skeleton 단계 무해, Electron 패키징(Week 9+) 단계 깨질 가능성
   - 결정: (a) ENVIRONMENT.md Node 22 pin 으로 갱신 + nvm 재설치 / (b) Electron 42 → 31 다운그레이드 / (c) Week 9 까지 deferral
   - **owner**: B 주관, A 의 ws_broadcaster 와 무관

6. **`c-review-notes.md` 미작성**
   - 데드라인: 2026-05-24 (A 의 v1.1-review-trigger 데드라인)
   - 4건 제안 양식 + 본인 코드 drift 4건 reference
   - **owner**: C — 즉시 작성 권장

### 8.3 정리·정책

7. **현 작업 `main` 브랜치 직접 커밋 vs devC PR 흐름**
   - role-assignment.md 정책: PRs from devA/devB/devC → main only
   - 현재 main 에서 직접 작업 — 정책 위반
   - 권장: 변경분을 devC 로 cherry-pick 또는 새 branch 로 옮긴 후 PR
   - **owner**: C 본인

8. **`origin/fuckdevC` 브랜치 잔류**
   - 비표준 명명 + 정리 안 됨
   - 권장: `git push origin --delete fuckdevC` (단 다른 사람이 사용 중인지 확인 후)

9. **`scripts/run-all.sh` doc sync**
   - 실제 코드는 patch 완료. plan/Velxor 측 `worker-C-timeline.md §2.5` 도 doc sync 완료 (2026-05-22 18:xx).
   - devC 트리의 `worker-C-timeline.md` 사본은 미동기일 가능성 — Week 3 collation 시 통합

---

## 9. C 본인 영역 review notes (요약)

### 9.1 본인이 짠 코드 self-review

`app.py` (22 LoC):
- ✅ `/health` 200 OK with `{status, model_version}` — `run-all.sh` 폴링 지원
- ✅ `/classify` 4-key 응답 (verdict, confidence, evidence, model_version)
- ⚠️ `request.get_json(silent=True) or {}` — body 유효성 검사 0건 (Week 1 stub 허용 범위)
- ⚠️ `MODEL_VERSION` 가 schema 컨벤션 (`lr-<ver>` 또는 `rules-fallback-<ver>`) 과 drift

`fallback_rules.py` (24 LoC):
- ✅ `Counter(event_type)` + `write_rate` 계산 합리적
- ✅ 4-key 응답 형태 일치
- ⚠️ `confidence=0.9` / `0.6` 하드코드 — Week 6+ 학습 모델 도입 시 재교정
- ⚠️ `MODEL_VERSION = "rule-based-v1"` schema drift

`waitress_conf.py` (5 LoC):
- ✅ threads=4 명시 — AC4 측정 재현성 유지
- 🟡 production-ready 가 아님 (logging X, 시그널 처리 X) — Week 8-9 안정화

`scripts/run-all.sh` (post-patch, 85 LoC):
- ✅ self-contained 가드 5종 (§11.1)
- ⚠️ `npm run dev` 손자 프로세스 trap 누락 — vite 좀비 가능 (별도 cleanup 명령 필요)
- ⚠️ engine venv 활성 가정 — `python-engine/.venv` 부재 시 명시적 에러 없이 fallback X. mkdir + python -m venv 자동 복구 추가 후보

### 9.2 본인 영역 외부 (A·B)에 대한 obs

A:
- 👍 `fanotify_adapter.rs` 가 Week 4-5 분량 선행됨 — schedule risk 완화
- 👍 ws_broadcaster 의 ReplayBuffer + last_seq=N 가 schema §4 와 정확히 일치
- ⚠️ `aggregator.rs` 가 빈 placeholder — Week 1 마감 전 wire-up 필요
- ⚠️ `main.rs` 가 classifier_client 를 import 안 함 → dead code warning (`#[allow(dead_code)]` 로 임시 무력화)
- 🟡 root 권한 정책 ADR 부재 — C/B 와 합의 필요

B:
- 🔴 App.tsx 본 구현 0% — Week 1 critical path 가장 큰 risk
- 👍 vite + react + electron + xyflow 의존성 설치는 완료
- 🟡 Electron 42 vs Node 20 호환 issue 사전 파악 필요

---

## 10. 다음 7일 계획 (Week 1 마감 → Week 2)

### 10.1 C 본인 (45h 중 ~6h 소비, ~39h 잔여)

| 우선 | 작업 | 예상 | 마감 |
|---|---|---|---|
| 🔴 1 | `c-review-notes.md` 작성 + PR comment 제출 | 1.5h | 2026-05-24 (A 데드라인) |
| 🔴 2 | run-all.sh patch + scripts/.gitkeep 정리 → devC PR | 0.5h | 2026-05-23 |
| 🔴 3 | `walking-skeleton-v1` tag push (조건: A·B 가 자기 머신에서 한 번씩 돌렸을 때) | 0.5h | 2026-05-25 |
| 🟡 4 | `app.py` model_version `"rule-based-v1"` → `"rules-v1"` rename (v1.1 발행 직후) | 0.5h | 2026-05-25 이후 |
| 🟡 5 | `requirements.lock` 발행 (`pip freeze > requirements.lock`) | 0.3h | Week 1 마감 |
| 🟢 6 | `worker-C-timeline.md` devC 트리 사본 doc sync (plan/Velxor 와 일치) | 0.5h | Week 3 collation |

### 10.2 A에게 C가 요청할 것

- root 권한 정책 ADR (옵션 A/B/C 중 결정) — Week 1 마감 전
- `aggregator.rs` Week 1 내 최소 sliding window + classifier_client.classify() 호출 wire-up
- `main.rs` 에 classifier_client 사용 흐름 추가 — `#[allow(dead_code)]` 제거 가능

### 10.3 B에게 C가 요청할 것

- Week 1 내 App.tsx 본 구현 시작 (최소 빈 ProcessTree placeholder + WS client 첫 줄)
- `ws/client.ts` 의 `?last_seq=N` query 처리 — schema §4.1 정확히 구현
- Node 22.12+ nvm 전환 여부 결정 (Electron 42 호환)

### 10.4 Week 2 (2026-05-29 ~) 예정

- C: rule-based classifier 초기 feature engineering (sliding window stats)
- C: classifier_client ↔ engine 한 사이클 end-to-end 측정 시도 (AC4 사전)
- A: aggregator 본 구현 + tracing 2-timestamp instrumentation
- B: ProcessTree + DetailPanel skeleton

---

## 11. 부록

### 11.1 `scripts/run-all.sh` 패치 요약 (5종 가드)

| 번호 | 가드 | 효과 | 코드 라인 |
|---|---|---|---|
| ① | `cd "$(dirname "$0")/.."` | 어디서 호출되어도 레포 루트에서 동작 | line 7 |
| ② | `mkdir -p rust-service/logs` | `cargo run 2> logs/trace.json` 리다이렉트 실패 방지 | line 14 |
| ③ | 포트 사전 점검 (8765/7000/5173) | 좀비 감지 시 명시적 에러 + 안내, auto-kill 안 함 (안전) | line 17-23 |
| ④ | `[ ! -d ui/node_modules ] && npm install` | fresh clone 자동 복구 | line 26-29 |
| ⑤ | 2단계 SIGTERM → 0.2s → SIGKILL trap | 좀비 잔류 방지 (단 `npm run dev` 손자는 별도 cleanup 필요) | line 38-50 |

### 11.2 운영 명령 모음

#### 좀비 일괄 정리
```bash
pkill -u $USER -f 'waitress_conf|cargo run|vite' 2>/dev/null
sleep 1
ss -tlnp | grep -E ':(8765|7000|5173)' || echo "✅ 모두 해방"
```

#### AC1 자동 검증 한 줄 (현 시점 — fanotify EPERM 우회)
```bash
cd /home/lsy/src/Velxor

# (1) ws-record 백그라운드 시작
mkdir -p ~/velxor-work
./scripts/ws-record.sh ~/velxor-work/ws-capture.jsonl > /tmp/ws-record.log 2>&1 &
WS_PID=$!

# (2) stub 모드 run-all 백그라운드 (fanotify 우회)
VELXOR_STUB=collector ./scripts/run-all.sh > /tmp/run-all.log 2>&1 &
RA_PID=$!

# (3) 6초 후 정리 + 판정
sleep 6
kill $WS_PID $RA_PID 2>/dev/null
pkill -u $USER -f 'waitress_conf|cargo run|vite' 2>/dev/null
sleep 1

# (4) ws-record.sh 가 자체적으로 PASS/FAIL 판정
grep -E 'AC1 (PASS|FAIL)' /tmp/ws-record.log
```

#### Python engine 단독 smoke
```bash
cd python-engine && source .venv/bin/activate
VELXOR_STUB=engine python waitress_conf.py &
sleep 1
curl -fsS -X POST http://127.0.0.1:8765/classify \
  -H "Content-Type: application/json" \
  -d '{"events":[],"window_ms":1000}'
# 기대: {"verdict":"ransomware","confidence":0.95,"evidence":["stub: ..."],"model_version":"stub-v1"}
pkill -f waitress_conf.py
```

#### Rust service 단독 smoke (stub 모드)
```bash
cd rust-service
VELXOR_STUB=collector cargo run --release &
sleep 2
ss -tlnp | grep :7000   # ws 서버 listen 확인
pkill -f target/release/rust-service
```

### 11.3 `walking-skeleton-v1` tag push 절차 (C 책임)

```bash
# A·B가 자기 환경에서 한 번씩 돌린 뒤
cd /home/lsy/src/Velxor
git fetch --all
# (현재 main 에 작업 중이면 devC 로 옮긴 후 PR → main merge)
git tag walking-skeleton-v1 -m "AC1: run-all.sh exit 0 + ws-record node_add{FileWrite} + 3인 재현"
git push --tags
```

### 11.4 변경 이력 (본 STATUS 문서 자체)

| 버전 | 일시 | 변경 | 발행자 |
|---|---|---|---|
| v1 | 2026-05-22 | 최초 작성. devC 트리 전수 검토 + AC1 65% / aggregator 미작성 / App.tsx boilerplate 문제 가시화 + C review 4건 초안 | C |

---

> **다음 status 갱신 권장 시점**: `walking-skeleton-v1` tag push 직후 (Week 1 마감) · 또는 v1.1 schema collation 종료 (Week 3 마감).
